# AuthioV OAuth client ID (seeded on the VoidAuth server)
VITE_VOIDAUTH_CLIENT_ID=authiov
# AuthioV app URL (used as the OAuth redirect_uri; must match VoidAuth's registered redirect URI)
VITE_APP_URL=http://localhost:5174
