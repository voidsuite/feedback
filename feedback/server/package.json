{
  "name": "voidfeedback-server",
  "version": "0.1.0",
  "private": true,
  "description": "Void Feedback gateway — VoidAuth session validation, REST API, SQLite store, realtime WebSocket push for the support & feedback hub",
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../.env --watch src/index.ts",
    "start": "bun --env-file=../.env src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.9.0",
    "nodemailer": "^6.10.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/nodemailer": "^6.4.17",
    "typescript": "~5.9.3"
  },
  "license": "MIT"
}
