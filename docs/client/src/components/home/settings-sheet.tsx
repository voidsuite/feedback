/**
 * Legacy alias — the settings UI is now a floating menu (settings-menu.tsx).
 * Kept so any old imports of `settings-sheet` still resolve.
 */

export { SettingsMenu as SettingsSheet } from "./settings-menu"