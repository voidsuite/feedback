services:
  voidauth-frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=${VITE_API_URL:-http://localhost:3001}
    container_name: voidauth-frontend
    ports:
      - "${FRONTEND_PORT:-80}:80"
    restart: always
