{
  "name": "m3il-server",
  "version": "0.1.0",
  "private": true,
  "description": "m3il gateway — OAuth proxy for VoidAuth, VoidAuth storage proxy, and SMTP/POP3 relay",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.9.0",
    "nodemailer": "^7.0.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/nodemailer": "^7.0.5",
    "typescript": "~5.9.3"
  },
  "license": "MIT"
}